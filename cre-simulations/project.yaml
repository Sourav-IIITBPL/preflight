# ==========================================================================
# CRE PROJECT SETTINGS FILE (cre-cli v1.3+)
# ==========================================================================
# This CLI version expects at least one target at project root.
# Use with:
#   cre workflow simulate ./my-workflow -T staging-settings ...
#
# Legacy metadata (kept for reference):
# id: preflight-defi-guard
# name: PreFlight DeFi Security Guard
# version: 1.0.0

staging-settings:
  rpcs:
    - chain-name: ethereum-mainnet-arbitrum-1
      url: https://arbitrum-one-rpc.publicnode.com

production-settings:
  rpcs:
    - chain-name: ethereum-mainnet-arbitrum-1
      url: https://arbitrum-one-rpc.publicnode.com
