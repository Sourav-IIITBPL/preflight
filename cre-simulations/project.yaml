# project.yaml — Chainlink CRE project definition
# Register this project: cre project create --file project.yaml

specVersion: v1
id: preflight-defi-guard
version: 1.0.0
name: PreFlight DeFi Security Guard
description: |
  Off-chain transaction simulation layer for Uniswap V2 swaps and ERC4626 vaults
  on Arbitrum. Complements on-chain guards with trace-based vulnerability detection.

workflows:
  - my-workflow/workflow.yaml